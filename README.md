# Train YOLO on Custom Dataset

A streamlined pipeline for training YOLO object detection models on custom datasets. 

## Project Structure
* `/data` - Dataset configurations and sample formats
* `/src` - Training and validation scripts
* `/runs` - Model weights and logs (git-ignored)

## Setup

pip install -r requirements.txt
